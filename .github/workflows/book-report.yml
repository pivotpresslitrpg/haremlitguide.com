name: Generate Book Report

on:
  schedule:
    - cron: '0 14 * * 4'  # Thursday 2pm UTC (weekly)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Generate book report
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/generate_book_report.py

      - name: Notify IndexNow
        if: success()
        run: |
          NEW_FILE=$(git diff --name-only HEAD~1 HEAD -- 'src/content/' | head -1)
          if [ -n "$NEW_FILE" ]; then
            SLUG=$(basename "$NEW_FILE" .md)
            curl -s -X POST "https://api.indexnow.org/indexnow" \
              -H "Content-Type: application/json; charset=utf-8" \
              -d "{\"host\":\"haremlitguide.com\",\"key\":\"hlg5d2e8f1a3b7c9\",\"urlList\":[\"https://haremlitguide.com/blog/${SLUG}/\",\"https://haremlitguide.com/blog/\",\"https://haremlitguide.com/sitemap-index.xml\"]}" || true
          fi

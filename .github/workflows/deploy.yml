name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 11 * * 2'   # Tuesday at 11am UTC
    - cron: '0 11 * * 4'   # Thursday at 11am UTC
    - cron: '0 11 * * 6'   # Saturday at 11am UTC
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Verify feed connectivity
        env:
          BLOG_FEED_API_KEY: ${{ secrets.BLOG_FEED_API_KEY }}
        run: |
          # Retry transient failures, and on a hard refusal print the JSON body: the
          # platform's key check and its bot defenses both answer 403, and only the
          # body says which one it was (Sept 2026: a rotated key looked like a block).
          body=$(mktemp); hdrs=$(mktemp)
          status=$(curl -s --retry 3 --retry-delay 5 --retry-all-errors -D "$hdrs" -o "$body" -w '%{http_code}' -H "X-Blog-Feed-Key: $BLOG_FEED_API_KEY" 'https://api.harem-lit.com/api/blog-feed/books?limit=2&sort=top_rated')
          echo "feed status: $status"
          if [ "$status" != "200" ]; then
            echo "Blog feed unreachable from runner (status $status) — failing instead of deploying an empty site."
            echo "response body (first 300 bytes): $(head -c 300 "$body")"
            echo "html title: $(grep -o '<title>[^<]*' "$body" | head -1)"
            echo "cloudflare verdict headers:"; grep -iE '^(server|cf-ray|cf-mitigated|cf-cache-status|content-type):' "$hdrs" || true
            exit 1
          fi

      - name: Build
        run: npm run build
        env:
          BLOG_FEED_API_KEY: ${{ secrets.BLOG_FEED_API_KEY }}

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist --project-name=haremlitguide --branch=main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Verify newest article on the custom domain
        run: |
          TODAY=$(date -u +%F)
          # Posts are drip-published: future-dated files exist in the repo but are
          # deliberately not built yet, so only date prefixes <= today can be verified.
          newest_published() {
            find src/content/posts -maxdepth 1 -type f -name '20*.md' | sort | awk -v t="$TODAY" -F/ '{ if (substr($NF,1,10) <= t) last=$0 } END { print last }'
          }
          CHANGED_FILE=$(git diff --diff-filter=AM --name-only HEAD^ HEAD -- 'src/content/posts/20*.md' | head -1)
          if [[ -n "$CHANGED_FILE" && "$(basename "$CHANGED_FILE" | cut -c1-10)" > "$TODAY" ]]; then
            CHANGED_FILE=""
          fi
          LATEST_FILE=${CHANGED_FILE:-$(newest_published)}
          test -n "$LATEST_FILE"
          SLUG=$(basename "$LATEST_FILE" .md)
          LATEST_ARTICLE_URL="https://haremlitguide.com/blog/${SLUG}/"
          curl --retry 12 --retry-delay 5 --retry-all-errors --fail --silent --show-error "$LATEST_ARTICLE_URL" -o /dev/null
          echo "LATEST_ARTICLE_URL=$LATEST_ARTICLE_URL" >> "$GITHUB_ENV"

      - name: Notify IndexNow
        if: success()
        run: |
          curl -s -X POST "https://api.indexnow.org/indexnow" \
            -H "Content-Type: application/json; charset=utf-8" \
            -d "{\"host\":\"haremlitguide.com\",\"key\":\"hlg5d2e8f1a3b7c9\",\"urlList\":[\"https://haremlitguide.com/for-professionals/\",\"https://haremlitguide.com/tags/\",\"https://haremlitguide.com/\",\"https://haremlitguide.com/lists/\",\"https://haremlitguide.com/new-releases/\",\"https://haremlitguide.com/blog/\",\"$LATEST_ARTICLE_URL\",\"https://haremlitguide.com/sitemap-index.xml\"]}" || true
